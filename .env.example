# Placeholder only. Prefer setting the key in the current PowerShell process.
EVREN_API_KEY=replace-in-process-environment-only
# DEBUG=1
# Optional rollback: native is the default; textual keeps the V1 prompt protocol.
# EVREN_TOOL_TRANSPORT=textual
